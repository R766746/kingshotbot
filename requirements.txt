# Runtime dependencies
#
# opencv-python-headless is used for template matching; the "headless"
# build avoids pulling in GUI libraries on servers. On a desktop you can
# swap it for opencv-python.
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
requests>=2.31

# Optional extras (uncomment if you want them):
# pytesseract>=0.3.10   # OCR (also requires the tesseract binary)
